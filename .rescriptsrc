const logConfig = config => {
  console.log(config)
  return config
}

const postcss = config => {
  require('react-app-rewire-postcss')(config, true);
  return config;
}

module.exports = [
  logConfig,
  postcss
]